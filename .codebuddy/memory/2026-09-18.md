# 2026-09-18

## 实施控制面三项修复（审计 `tmp/control-panel-audit.md` 的落地）

### 1. 止血：评审单点脏数据不再拖垮整页
- `app.py` 新增 `_degraded_review_payload(entry, exc)` + `_degraded_review_entries(reviews)`。
- `_review_inbox_payload` 逐 run 包 `try/except (ReviewIdentityMismatchError, ArtifactProjectionReadError)`，降级为 `status="review_unavailable"`、`review_id="unresolved:<run_id>"`、带 `error{code,message}` 的条目。
- `_console_review_panel` 同样降级（返回带 `present/packet/error` 的字典），不再让选中 run 的评审异常冒泡。
- `/console/state` 与 `/reviews` 新增 `degraded: {active, review_entries[]}`。
- `_console_artifact_panel` 不再把真实错误码吞成 `manifest_unreadable`，改为透传 `exc.code/exc.message`。
- 前端 `console.html`：新增 `#console-degraded` 横幅 + `renderDegraded()`；`renderReviewInbox` 对 `review.error` 渲染 `.degraded-item`（不给链接）；`renderReviewPanel` 对 `panel.error` 面板级渲染并 `resetReviewDecisionForm()`；`renderArtifactPanel` 改为 `else if (panel.error)`。

### 2. 恢复自愈：握手不再依赖残留 cookie
- `app.py` 注入脚本：守卫从 `if (document.cookie.includes("ai_actuary_csrf=")) return;` 改为 `async` 探测 `nativeFetch("/console/state")`，仅 401/403 时 `clearStaleSessionCookies()`（清 `ai_actuary_csrf` 与 `ai_actuary_operator_session`）并显示面板。
- `capabilities.py`：`exchange_bootstrap` 自己置 `_bootstrap_used = True`（保持直连 token 单次 + TTL 过期）；`create_bootstrap_handoff`/`approve_bootstrap_handoff` **不再**检查 `_bootstrap_used` 与 `_bootstrap_expires_at`，handoff 窗口改为进程生命周期（handoff_id 自身仍有 TTL）；`_issue_session` 不再置 `_bootstrap_used`。

### 3. 除根：产物目录按 run 隔离
- `_default_artifact_dir(settings, case_id, run_id=None)` → `<artifact_root>/<case_id>/<run_id>`。
- `create_run` 顶部先 `run_id = _generate_api_run_id(case_id)`，三条分支（workflow / model-tool / 默认）统一复用该 run_id，原来各分支各自生成导致目录名与 run_id 不一致。
- rerun：函数顶部 `rerun_id = _generate_api_run_id(...)`，三个分支均强制新目录（不再复用 `entry["artifact_root"]`）；`rerun_from_registry` 新增 `new_run_id` 参数，使目录名 == run_id。

## 验证
- 全量 `pytest -q`：**1048 passed / 4 failed**（4 个是 MEMORY 里记录的既有环境失败：`test_tool_contract_compat_manifest.py` ×2、`test_adk_developer_foundation.py` ×2）。
- 改了两个测试来匹配新契约：`test_control_plane_capabilities.py` 的 handoff 过期用例改为「handoff_id 仍会过期 + 过期后可重新申请」，新增 `_assert_identity_conflict_response()` 替换 `test_api_control_plane.py` 里两个 409 断言（聚合面现在 200+degraded，单条面仍 409）。
- `test_operator_console_assets.py` 的三个 golden 常量重算：字符 57091 / 字节 57119 / sha256 `f02ffe401eabb3ac52d60dcafce22680d8decb5ec8d3832f866fbfbed7b7c278`。
- 冒烟脚本：`tmp/smoke_console_state.py`（真实 `tmp/` 数据下 `/console/state` 从 409 → 200）、`tmp/smoke_artifact_dir.py`（目录布局 + 注入脚本新守卫已生效）。
- 前端与注入脚本均通过 `node --check`。

## 行尾经验补充
- `app.py` / `tests/test_api_control_plane.py` 必须用字节级脚本改：读 `bytes.replace(b"\r\r\n", b"\n")` → 文本替换 → 写回 `.replace("\n", "\r\r\n")`。已验证 `cr == 2*lf` 是干净判据。

## 第二轮：experience study 进 review + 可配置 AI reviewer（已落地）
- `model_tools/runner.py`：新增 diagnostics 计算与 `evaluate_experience_study_constitution()`，走同一个 `evaluate_case_constitution`；默认阈值 `{zero_denominator_count:0, low_credibility_count:0, max_ae_ratio:5}`；`needs_review` 时生成 review packet 并调 AI review。engine 的 hard constraint 现在认 `metadata["tool_id"]`。
- `review/ai_reviewer.py`（新）：OpenAI 兼容 SDK 调用，落 `ai_review.json/.md`，回写 packet 的 `ai_review`/`ai_suggestion`；`operator_entrypoint._attach_ai_review` 接在 delivery 之前。
- 实测坑：reasoning 模型 `max_completion_tokens` 不足会返回空 JSON → 加"空响应重试一次"，仍空记 `empty_model_response`；默认 max_tokens 从 1200 提到 4000。

## 第三轮：reviewer 换 DeepSeek + 文档体系（已落地）
- `.env` / `.env.sample` 全英文、结构对齐；reviewer 默认 `deepseek-chat` + `https://api.deepseek.com/v1`，`AI_ACTUARY_REVIEW_API_KEY` 未设时自动回退 `DEEPSEEK_API_KEY`（模型/端点含 deepseek 时），否则 `OPENAI_API_KEY`。实测 DeepSeek reviewer 正常（英文包 → 英文输出）。
- 新增文档：`README.zh-CN.md`、`docs/operations-manual.md` + `.zh-CN.md`（含两条链路的 mermaid 图、逐步交接表、review/AI review 来源、配置表、排障）、`docs/adk-operations-manual.md` + `.zh-CN.md`（ADK 是什么/怎么配/工具三组/执行规则/禁用项）；`docs/README.md` 索引同步。
- **坑**：`tests/test_documentation_handoff.py` 要求 README 必须出现字面词 `Calculation Core`、`OpenAI Planner`、`Hermes Workers`、`Step-by-Step Operating Guide`、`Human Responsibilities vs Agent Responsibilities` 等 —— 重写 README 表格时不能把这些词删掉。
- **坑**：按 `.env.sample` 重建 `.env` 时，中文注释行里的 `>=`、`override=False` 会被误判成键值 → parse 必须校验 `^[A-Za-z_][A-Za-z0-9_]*$` 且值不含空格/`#`；`OPENAI_BASE_URL=` 空值必须保持注释（否则 SDK 请求 URL 缺协议）。
- 服务已重启（8000: 30628 / 8001: 33416，日志 `tmp/wb-0918g.*.log`，bootstrap token `local-dev-bootstrap-0918`）。

## 第四轮：执行器侧叙述生成做成可选模型位（已落地）
- 新增 `src/reserving_workflow/llm_client.py`（公共 OpenAI 兼容 chat helper：`resolve_llm_settings`、`chat_json`（空响应重试一次 + `max_completion_tokens`/`max_tokens` 双参数容错）、provider key 回退），`review/ai_reviewer.py` 已改为复用它（行为不变）。
- 新增 `src/reserving_workflow/ai_narrative.py`：`NarrativeDraftWriter.draft_text()` + `narrative_llm_settings()`（`AI_ACTUARY_NARRATIVE_*`，默认 **关闭**）+ `draft_narrative_dict()`（experience study 用）。护栏：`numbers_supported()` 校对文中数字必须来自证据（支持少写小数位、年份豁免、千分位、`case-42` 负号），违者降级模板。
- `narrative.py` 拆成 `build_narrative_draft()`（兼容旧签名）+ `build_narrative_draft_with_meta()`；`cited_values` 永远来自确定性结果。Hermes `case_worker` 注入 writer 并把 `narrative_source`/`narrative_model` 写进 `worker_metadata`；CLI `narrative-draft` 加 `--narrative-model auto|on|off`，meta 写字侧车 `narrative_draft.meta.json`。
- 实测：用 DeepSeek（`deepseek-chat`）给 RAA 写叙述，护栏通过 `source=model`；数字全部引用自证据。
- 文档：`README` 中英（模型分工表新增 Executor/narrative 行 + 新小节）、`docs/operations-manual` 中英新增第 8 节（护栏与降级表、配置、排障）。
- 新增 `tests/test_ai_narrative.py`（12 用例全过）；全量回归仅剩既有环境失败（compat manifest ×2、adk ×3、Windows 句柄用例各 1；adk forced-launcher 用例在无 `.env` 时同样失败 → 与本轮改动无关）。
- 服务已重启（8000: 42056 / 8001: 44572，日志 `tmp/wb-0918h.*.log`）。

## 批测：叙述模型位措辞质量（8/8 通过）
- 评测脚本 `tmp/batch_narrative_check.py`（可复跑，支持 `--models deepseek-chat,gpt-5.6-luna` 与 `--only <case-substr>`）：跑内置 case pack 3 例（两个模拟三角 + RAA）+ 1 个极小稀疏三角（最易诱发编数），每例两模型。
- 结果：`source=model` 8/8，`numbers always intact: True`（`cited_values` 全等于确定性结果），无一例触发 `numeric_guard_rejected`。
- 发现并修好的质量问题：模型有时**逐字复述模板 summary**（那时 `rewrote=False`）→ 在 `_SYSTEM_PROMPT` 加第 5 条"不要照抄模板，用新措辞表达同样事实"，并把语言规则顺延为第 6 条；复测后 8/8 `rewrote=True`。
- 踩坑：手工 `triangle_rows`（3 行小三角）转不成 chainladder Triangle → 压测小三角改用 `evaluation.simulation.build_simulated_case_payload(simulation={...origin_count:3, development_count:3...})` 生成。
- 未改动 `.env` 默认值：`AI_ACTUARY_NARRATIVE_ENABLED` 仍是 `0`（默认确定性）；批测用进程级 env 打开。
- **更正（同日稍后）**：用户拍板"每次都开启"，已固化为默认开启，上一行的"保持 0"作废。

## 叙述模型位固化为默认开启（2026-09-18 晚）
- 代码：`ai_narrative.py` 的 `resolve_llm_settings(default_enabled="1")`（原 `"0"`）+ 两处 docstring；`.env.sample` 与本机 `.env` 均 `AI_ACTUARY_NARRATIVE_ENABLED=1`。
- 测试：`test_ai_narrative.py` 的 `test_narrative_slot_is_disabled_by_default` → 改为 `test_narrative_slot_is_enabled_by_default`，并新增 `test_narrative_slot_can_be_disabled`（`=0` 时关闭）。
- 文档：README.md / README.zh-CN.md（角色表 "Executor 是否调用模型"、模型位配置表默认列、特性清单新增一条、"Narrative drafting / 叙述生成"小节改为"默认开启 + 用 `0`/`--narrative-model off` 关闭"）；operations-manual.md / .zh-CN.md（配置表默认列、§8 开头、bash 示例加关闭注释）。
- 验证：`tmp/verify_narrative_default.py`（CLI 工具链，不设任何 `AI_ACTUARY_NARRATIVE_*` → `narrative_draft.meta.json` 里 `source=model`，`cited_values` 与确定性结果一致）；`tmp/run_demo_review.py`（完整 planner+worker 链路）→ `narrative_draft.json` 的 summary 为模型措辞。
- 服务已重启（冷启动要等 ~40s，12s 不够；日志 `tmp/wb-0918i.*.log`），`/health` 正常。
- 全量 pytest：1060 passed / 5 failed，失败的仍是既有环境问题（ADK launcher 2 个 + `test_developer_agent_is_code_first_gemini...` + compat manifest 行尾 2 个），与本次改动无关。
- 大文件/多文件改文案统一用字节级补丁脚本 `tmp/patch_narrative_default.py`（保留各文件原有行尾：README.md 与 operations-manual.zh-CN.md 是 CRLF，其余 LF）。

## DeepSeek 模型 id 改为 `deepseek-flash`（2026-09-18 晚）
- 用户要求：`deepseek-chat` → `deepseek-flash`（视为简单操作，未跑 pytest）。
- 统一替换 18 处（脚本 `tmp/patch_deepseek_model.py`，字节级保留行尾）：`.env`、`AI_ACTUARY_ADK_MODEL` 已是 `deepseek/deepseek-flash`（未动）、`.env.sample`、`README.md`、README.zh-CN.md、`docs/operations-manual.md`(4)、`operations-manual.zh-CN.md`(4)、`adk-operations-manual.md`(→`deepseek/deepseek-flash`)、`.zh-CN.md`、`tests/test_ai_narrative.py`(2)、`tmp/batch_narrative_check.py` 默认 `--models`。
- 源码默认值无需改：`DEFAULT_REVIEW_MODEL` / `DEFAULT_NARRATIVE_MODEL` 都是 `gpt-5.6-luna`；key 回退靠 `_PROVIDER_KEY_HINTS` 的 `deepseek` 子串，`deepseek-flash` 仍命中，无需改代码。
- 残留（属历史产物，未改）：`tmp/deepseek-review-check/*`、`tmp/operator-runs/demo-review-threshold-5/*` 里记录的当时模型 id。

## 服务重启（模型改名后，供用户实测）
- 杀旧进程后重启 `scripts/run_local_workbench.py`：Console/API `8000`=PID 13884、ADK `8001`=PID 28640；日志 `tmp/wb-0918i.out.log` / `.err.log`。
- 启动前设了 `$env:AI_ACTUARY_OPERATOR_BOOTSTRAP_TOKEN`（避免无 tty 时握手卡住）；父进程是 Start-Process 起的 launcher，用 Job Object 管进程树 → 重启时按端口找 PID 杀即可。

## 踩坑：握手 401 bootstrap_invalid（已修）
- 现象：`python tmp/approve_handoff.py <handoff_id>` 返回 401 `bootstrap_invalid`。
- 根因：我启动时用随机 token 设了 `$env:AI_ACTUARY_OPERATOR_BOOTSTRAP_TOKEN`，**覆盖了 `.env` 里已有的 `dev-bootstrap-0918`**（`app.py` 环境变量优先），而脚本硬编码 `dev-bootstrap-0918` → 不匹配。旧进程的 handoff_id 也随之失效（handoff 窗口 = 进程生命周期）。
- 修：`tmp/approve_handoff.py` 改为 `os.environ.get("AI_ACTUARY_OPERATOR_BOOTSTRAP_TOKEN", "dev-bootstrap-0918")`；重启时**不要随机生成**，直接用 `.env` 的 `dev-bootstrap-0918`（或设成同一固定值）。自检 `python tmp/approve_handoff.py`（无参数）= request+approve 全 200 即通道正常。
- 用户侧流程：浏览器控制台点 "Request launcher handoff" 拿**当前进程**的 handoff_id → `python tmp/approve_handoff.py <handoff_id>` → 页面自动 claim。
- **pytest 报的行号是该文件的 2 倍**（Python 把 `\r` 也当换行）；ripgrep/归一化后的行号才是真实行号。

## 清理与起服务（供用户实测）
- 删掉本次会话的一次性脚本（patch_app*.py / patch_test.py / dump_*.py / eol_check.py / verify_app.py / *_console.js / *_excerpt.txt）；保留 `tmp/control-panel-audit.md`、历史诊断脚本与 `tmp/smoke_console_state.py`、`tmp/smoke_artifact_dir.py`。
- 停掉上一轮的旧进程（launcher 39308 + uvicorn 44756 + adk 9400）后重启，避免测到旧代码。
- 新版 workbench 已起：8000 = API/Console，8001 = ADK；日志 `tmp/wb-0918.out.log` / `tmp/wb-0918.err.log`，固定 bootstrap token `dev-bootstrap-0918`。
- `tmp/approve_handoff.py` 改成可传参：无参自检 token，带 handoff_id 则批准控制台握手。

## .env.sample
- 新增仓库根 `.env.sample`（分组：模型凭证 / ADK 面 / 控制面凭证与握手 / 地址与 TTL / 烟测评估），并在 `.gitignore` 加 `!.env.sample`（原 `.env.*` 会把它忽略掉）。
- env 全量清单来自脚本扫描 `os.environ.get|getenv|environ[...]`（`src`、`scripts`、`developer_workflows`、`workflows`、`benchmarks`）：`OPENAI_API_KEY`、`OPENAI_BASE_URL`、`GOOGLE_API_KEY`、`DEEPSEEK_API_KEY`、`AI_ACTUARY_ADK_MODEL`（默认 `deepseek/deepseek-v4-pro`）、`AI_ACTUARY_CONTROL_PLANE_URL`、`AI_ACTUARY_ADK_CREDENTIAL`、`AI_ACTUARY_OPERATOR_CREDENTIAL`、`AI_ACTUARY_OPERATOR_BOOTSTRAP_TOKEN`、`AI_ACTUARY_OPERATOR_ORIGIN`、`AI_ACTUARY_ADK_URL`、`AI_ACTUARY_OPERATOR_SESSION_TTL`（默认 900，上限 18000）、`AI_ACTUARY_OPERATOR_BOOTSTRAP_TTL`（默认 300，上限 3600；launcher 覆盖为 1800）、`AI_ACTUARY_BROWSER_SMOKE_RUNNER`、`AI_ACTUARY_REAL_MODEL_EVAL_MODE`。
- 坑：现有 `.env` 里的 `BOOTSTRAP_TOKEN` 是历史遗留键名，代码不读（代码读 `AI_ACTUARY_OPERATOR_BOOTSTRAP_TOKEN`）。
- 坑：三个 capability secret 必须"同时提供或同时不提供"，否则 `create_app` 直接 raise；launcher 会自动补随机值，直接跑 uvicorn 才要自己填。
- 注释语言按用户要求改为**英文**（项目文档也是英文）。
- 用户的 `.env` 已写入三个本地能力凭证的固定值（值不记录到 memory；`AI_ACTUARY_OPERATOR_BOOTSTRAP_TOKEN` 固定为 `dev-bootstrap-0918`，另两个是 `local-operator-*` / `local-adk-*` 随机串）。这样重启后不用再手填，且非 tty 启动时仍能用 `tmp/approve_handoff.py <handoff_id>` 批准握手。
- 提醒：`.env` 只在 launcher 启动时 `load_dotenv` 一次，改完必须重启 workbench 才生效。
- 已用新 `.env` 重启（uvicorn 19800+ / adk 596），自检 `python tmp/approve_handoff.py` 返回 200，确认 `.env` 里的 bootstrap token 生效；ADK 侧日志也显示 `Loaded .env file for ai_actuary_developer`。日志 `tmp/wb-0918b.out.log` / `.err.log`。
- `GOOGLE_API_KEY` 指的是 **Google AI Studio 的 Gemini API key**（GoogleAI 后端，https://aistudio.google.com/apikey，形如 `AIza...`），不是 GCP 服务账号/OAuth。Vertex 路线要用 `GOOGLE_CLOUD_PROJECT` + `GOOGLE_CLOUD_LOCATION` + ADC（`GOOGLE_APPLICATION_CREDENTIALS`）；`GOOGLE_GENAI_USE_VERTEXAI` 在 adk 2.7.1 已废弃，改名 `GOOGLE_GENAI_USE_ENTERPRISE`。
- 本项目里 `GOOGLE_API_KEY` 只被 `evaluation/adk_lanes.py:110/141` 用来判断"凭证是否存在"和打 provider 标签，不真正调用 Google API；默认模型是 deepseek，因此该项留空不影响运行。
- 排查 `20260918_demo_1` 运行失败（status=failed / `planner_runtime` / `Connection error.`）：根因是 `.env` 中 `OPENAI_BASE_URL=` 空值（详见 MEMORY.md）。已注释该行 + 更新 `.env.sample` 警告，重启服务（uvicorn 5912 / adk 8712，日志 `tmp/wb-0918c.*.log`）。
- 模型默认值统一为 `gpt-5.6-luna`：planner 从硬编码 `gpt-4.1-mini` 改为 env 驱动 `AI_ACTUARY_PLANNER_MODEL`；ADK 默认从 `deepseek/deepseek-v4-pro` 改为 `gpt-5.6-luna`。已写入 `.env` 与 `.env.sample`（新增 "2) Model selection" 小节），重启后生效（uvicorn 25536 / adk 26300，日志 `tmp/wb-0918e.*.log`）。已验证该模型在当前 key 下可调用。
- 实现两项（已改码、已验证）：① experience study 纳入 review 链路（diagnostics + constitution + review packet，默认阈值零分母/低可信度/A-E 上限，可用 `review_thresholds` 覆盖）；② 新增可配置 AI review（`review/ai_reviewer.py`，模型/ provider 独立于 planner，`.env` 用 `AI_ACTUARY_REVIEW_*`）。两者都只在 `needs_review` 时触发，产物 `ai_review.json/.md` 并回写 packet。服务已重启（uvicorn 42756 / adk 28768，日志 `tmp/wb-0918f.*.log`，bootstrap token `local-dev-bootstrap-0918`）。
- 调研（未改码）：AI review 当前是**确定性**流程（constitution 阈值/产物完整性 → `needs_review` → 生成 review_packet），不调用任何模型；experience study 走 `MODEL_COMPARISON_TOOL_RUNNERS` 绕过该链路，所以没有 review。详见 MEMORY.md「Review 链路」。
- 偶发：`Start-Process` 启动 launcher 后进程秒退（日志文件 0 字节、诊断日志无记录），重试一次即正常 —— 启动后务必复查端口与进程，别假设成功。
- 重启踩坑：`Get-CimInstance Win32_Process -Filter "Name='python.exe'"` 配合 `Where-Object CommandLine -like` 有时会漏查；**以 `Get-NetTCPConnection -State Listen -LocalPort 8000,8001` 为准**。旧进程没杀干净时新启动会报 `Loopback port conflict on 8000, 8001; no child process was started.`，且端口仍显示旧 PID。
