# Benchmark Cases

Store benchmark case definitions here. Each case should eventually include:

- case identifier,
- reserving scenario type,
- triangle inputs,
- context metadata,
- expected governance behavior.
