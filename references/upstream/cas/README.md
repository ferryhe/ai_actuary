# CAS Upstream Snapshot

This folder retains only the upstream CAS-side materials that are still useful as technical reference inputs for this repo.

## Retained reference scope

- `benchmarks/` — benchmark package notes and case/rubric structure
- `docs/architecture.md` — upstream architectural framing
- `workflows/agent-runtimes/README.md` — upstream runtime/workflow notes

## Removed scope

Application and submission materials were intentionally removed from this repo snapshot. This project keeps implementation-facing reference inputs only, not proposal-packaging history.

## Usage note

Treat everything here as read-only context. Active implementation lives in this repository's own `src/`, `workflows/`, `scripts/`, and `docs/` trees.
